const basket = ['apples','grapes','pears']


// Linked List: apples --> grapes --> pears
/**
 *  preprend o(1)
 *  append o(1)
 *  lookup o(n)
 *  insert o(n)
 *  delete o(n)
 */

//pointer - reference to another place in memory
const obj1= {a: true};
const obj2 = obj1;
// obj2 point to the memory space of {a:true}
